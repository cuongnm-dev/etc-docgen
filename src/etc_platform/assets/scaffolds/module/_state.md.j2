---
feature-id: {{ module_id }}
feature-name: {{ module_name | tojson }}
pipeline-type: sdlc
status: in-progress
depends-on: {{ depends_on | tojson }}
blocked-by: []
created: "{{ created_at }}"
last-updated: "{{ created_at }}"
current-stage: ba
output-mode: {{ output_mode | default('lean') }}
repo-type: {{ repo_type }}
repo-path: "."
project: {{ primary_service | default('') | tojson }}
docs-path: docs/modules/{{ module_id }}-{{ slug }}
intel-path: docs/intel
stages-queue: {{ stages_queue | tojson }}
completed-stages:
  doc-intel:
    verdict: "Ready for BA"
    completed-at: "{{ created_at }}"
kpi:
  tokens-total: 0
  cycle-time-start: "{{ created_at }}"
  tokens-by-stage: {}
rework-count: {}
locked-fields: []
agent-flags: {{ agent_flags | default({}) | tojson }}
feature-req: |
  file:docs/modules/{{ module_id }}-{{ slug }}/module-brief.md
  canonical-fallback:docs/intel/_snapshot.md
  scope-modules: {{ modules_in_scope | tojson }}
  scope-features: []
  dev-unit: ""
clarification-notes: ""
---

# Pipeline State: {{ module_name }}

## Business Goal

{{ business_goal | default('[CẦN BỔ SUNG: 1-2 câu mô tả mục tiêu nghiệp vụ của module]') }}

## Stage Progress

| # | Stage | Agent | Verdict | Artifact | Date |
|---|---|---|---|---|---|
| 1 | Intake | doc-intel | Ready for BA | docs/intel/_snapshot.md | {{ created_at }} |
{% for stage in stages_queue -%}
| {{ loop.index + 1 }} | {{ stage }} | {{ stage }} | — | — | — |
{% endfor %}

## Current Stage

**ba** — Ready to start. Input: `docs/modules/{{ module_id }}-{{ slug }}/module-brief.md`.

## Next Action

Cursor: `/resume-module {{ module_id }}` để dispatch BA agent.

## Active Blockers

none

## Wave Tracker

| Wave | Tasks | Dev Status | QA Status |
|---|---|---|---|

## Escalation Log

| Date | Item | Decision |
|---|---|---|
