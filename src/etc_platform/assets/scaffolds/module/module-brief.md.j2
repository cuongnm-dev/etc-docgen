---
module-id: {{ module_id }}
module-name: {{ module_name | tojson }}
slug: {{ slug }}
canonical-source: docs/intel/_snapshot.md
generated-at: "{{ created_at }}"
generator-version: "v1-mcp"
scope:
  modules: {{ modules_in_scope | tojson }}
  features: []
  depends-on: {{ depends_on | tojson }}
metrics:
  features-in-scope: 0
  primary-service: {{ primary_service | default('') | tojson }}
  total-entities-in-scope: 0
  total-rules-in-scope: 0
---

# Module Brief: {{ module_name }}

## Scope

| Dimension | Value |
|---|---|
| Module ID | {{ module_id }} |
| Slug | {{ slug }} |
| Primary service | `{{ primary_service | default('—') }}` |
| Depends-on | {{ depends_on | join(', ') if depends_on else 'none' }} |
| Modules in scope | {{ modules_in_scope | join(', ') if modules_in_scope else '—' }} |

## Features in scope

(populated by `scaffold_feature` calls)

## Business Rules (scoped)

| ID | Rule | Type | Applies-to | Severity | Source |
|---|---|---|---|---|---|

## Entities + Relationships (scoped)

```yaml
entities: []
relationships: []
state-machines: []
```

## NFRs Applicable

| Area | Requirement | Target | Source |
|---|---|---|---|
| Performance | | | |
| Security | | | |
| Reliability | | | |
| Audit/Logging | | | |

## § Agent Hints (Opus-precomputed)

### § for ba

(populated during BA stage)

### § for sa

(populated during SA stage)

### § for tech-lead

(populated during tech-lead stage)

### § for dev

(populated during dev stages)

### § for qa

(populated during QA stage)

### § for reviewer — DoD checklist

(populated during reviewer stage)
